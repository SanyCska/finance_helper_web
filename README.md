# Finance Helper — Mini App

Telegram Mini App личного финансового помощника. Бэкенд лежит рядом:
`../finance_helper_back`, там же docker-compose и инструкция по боту.

## Запуск

```bash
npm install
npm run dev
```

Аппа поднимется на `http://localhost:3010` и будет ходить в API на свой же origin:
Next проксирует `/api/*` в бэкенд. Адрес бэкенда задаётся переменной
`API_PROXY_TARGET` (по умолчанию `http://127.0.0.1:8010`).

Так сделано ради Telegram: страницу отдаёт https-туннель, и прямой запрос
на `http://localhost` браузер заблокировал бы как смешанное содержимое. С прокси
достаточно одного туннеля на порт фронтенда, а CORS не нужен вовсе.

Вне Telegram приложение работает как обычная веб-страница — удобно для отладки.
Чтобы бэкенд пускал такие запросы, включите на нём `DEV_BYPASS_AUTH=true`
(флаг игнорируется при `ENV=production`).

Внутри Telegram запросы подписываются `initData`, который клиент кладёт
в заголовок `Authorization: tma <initData>`.

## Экраны

| маршрут | что показывает |
|---|---|
| `/` | сальдо месяца, донат трат, топ категорий, последние операции |
| `/transactions` | операции по дням с фильтрами по категориям и поиском |
| `/add` | добавление траты вручную |
| `/categories` | разбивка месяца по категориям |
| `/category/[name]` | динамика одной категории по месяцам |
| `/compare` | сравнение двух месяцев |
| `/plan` | строки плана, зарплата месяца, ожидаемое сальдо |
| `/plan/vs-fact` | план против факта, построчно и по категориям |
| `/plan/recurring` | подписки и аренда: месячная доля и итог за месяц |
| `/funds` | источники средств, общий итог в долларах и его динамика |
| `/funds/check` | сверка месяца: реальное сальдо против учтённого |

Выбранный месяц живёт в строке запроса (`?month=2026-07`), поэтому переживает
переходы между экранами и возврат по кнопке «назад».

### Средства и сверка

Источник средств — место, где лежат деньги: счёт, карта, наличные, брокер. Каждое
изменение суммы пишется отдельным снимком, поэтому по источнику видна динамика,
а общий итог — сумма последних снимков, пересчитанных в доллары.

Раз в месяц приложение предлагает сверку: сравнивает, насколько выросли реальные
остатки, с сальдо по введённым доходам и тратам. Разница — погрешность ведения;
подтверждённая сверка фиксируется и позднейшими правками балансов не переписывается.

### Подписки и аренда

Одна сущность с полем «вид»: механика у них общая. В траты попадает не платёж,
а его месячная доля — годовая подписка делится на двенадцать, — и начисление
создаётся последним днём завершившегося месяца. Правка суммы действует на текущий
и будущие месяцы, уже начисленные не пересчитываются.

### План

Строка плана может быть в долларах или евро и связана с категорией трат: тогда
на вкладке «План vs факт» рядом со строкой видно, сколько по этой категории ушло
на самом деле. Итоги всегда в долларах. Месяц без своего плана показывает черновик
из плана прошлого месяца, а зарплата переносится вперёд, пока не введена новая.

## Устройство

```
app/          маршруты Next.js (App Router) и глобальные стили с токенами дизайна
components/   экраны и переиспользуемые блоки, включая графики на голом SVG/CSS
lib/          формат сумм и дат, клиент API, обёртка Telegram, цвета категорий
tests/        Vitest: форматтеры, геометрия доната, цвета
```

Графики нарисованы вручную: донат — через `conic-gradient`, столбцы и полосы — через
flex. Библиотека чартов потребовала бы больше кода на переопределение стиля
(нулевые радиусы, двухпиксельные линейки, `tabular-nums`), чем сама отрисовка.

### Шрифты

Мокапы сделаны на Archivo, но у неё нет кириллицы. Archivo оставлен для цифр и латиницы
— на них держится вся типографика сумм, — а кириллица падает на Inter: тот же
нейтральный гротеск с начертанием 800. Оба шрифта подключаются через `next/font`,
внешних CDN в рантайме нет.

## Проверки

```bash
npm run test
npm run typecheck
```

`npm run build` не запускайте, пока работает `npm run dev`: обе команды пишут
в один каталог `.next`, продакшен-сборка перетирает манифесты дев-сервера,
и он падает с `__webpack_modules__[moduleId] is not a function`. Лечится
остановкой сервера, `rm -rf .next` и повторным запуском.
